# Enforce the Node/npm versions declared in package.json "engines".
engine-strict=true

# Use a clean, reproducible install by default in CI/local.
# (does not affect `npm install`; documents intent)
save-exact=false
